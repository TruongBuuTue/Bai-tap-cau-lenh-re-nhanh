#include "stdio.h"
int main(){
	// Bai tap nhap vao so nguyen bat ki va hien thi so nguyen do la so am
	int a;
	printf("Nhap vao a = "); // a la so nguyen bat ki
	scanf("%d", &a);
	// Hien thi a la so am
	if (a<0){
		printf("%d la so am", a);
	}
	
}
